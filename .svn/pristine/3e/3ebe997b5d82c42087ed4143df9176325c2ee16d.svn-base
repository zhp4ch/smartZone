package com.mass.biz.smart.adConfiguration.service; 

import java.util.List;

import com.mass.biz.smart.adConfiguration.model.AdConfiguration;

/**
 * 类描述：广告配置服务层
 * 创建人：yihai Zhao
 */
public interface AdConfigurationService {

	AdConfiguration addAdConfiguration(AdConfiguration adConfiguration);
	
	boolean editAdConfiguration(AdConfiguration adConfiguration);
	
	List<AdConfiguration> selectList(AdConfiguration adConfiguration);
	
	boolean editAdConfByType(Integer type);
	
	boolean delAdConfiguration(Integer type);
	
	AdConfiguration selectAdConfig(Integer state);
}
